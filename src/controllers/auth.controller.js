import User from '../models/User';
// Controller Auth

export const signIn = ( request, response ) => {
    response .json( 'singIn' );
}

export const signUp = ( request, response ) => {
    response .json( 'singUp' );
}